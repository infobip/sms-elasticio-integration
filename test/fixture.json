{
    "fixtures":{
        "send":{
            "msg":{
                "body":{
                    "senderAddress": "{{infobip_sender_address}}",
                    "address":  "{{infobip_phone_number}}",
                    "message": "{{infobip_message_text}}"
                } 
            },
            "cfg":{ 
                "username": "{{infobip_username}}",
                "password": "{{infobip_password}}"
            }
        },
        "receive":{
            "msg":{
                "headers":{
                    "Content-Type": "application/json"
                },
                "body":{
                } 
            },
            "cfg":{ 
                "username": "{{infobip_username}}",
                "password": "{{infobip_password}}"
            }
        }
    }
}